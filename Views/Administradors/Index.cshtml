@model IEnumerable<AspnetCoreMvcFull.Models.Administrador>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AreaTrabajo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AreaTrabajo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuarioNavigation.IdUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAdministrador">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAdministrador">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAdministrador">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
